import SortBar from "./PageComponents/SortBar";

function Walk() {
    return (
      <div className="walk">
        <header className="pageTitle">
          WALK
        </header>
        <SortBar />
      </div>
    );
}
  
export default Walk;